<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Item" parent="">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Item.Share">
        <item name="android:layout_width">@dimen/item_circle_size_share</item>
        <item name="android:layout_height">@dimen/item_circle_size_share</item>
        <item name="android:textSize">@dimen/item_font_size_share</item>
        <item name="android:textColor">@color/share_primary</item>
    </style>


</resources>
